steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '-f=docker/Dockerfile'
  - '--tag=gcr.io/${PROJECT_ID}/aag-weather'
  - '.'

# aag-weather is pushed to Container Registry
images:
- 'gcr.io/$PROJECT_ID/aag-weather'
